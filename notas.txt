sw              lista de stopwords
test            función que regresa una lista con las palabras de un renglón
lines           lista con lista de renglones del texto
dict            diccionario {'palabra':total de veces}
top_words       diccionario ordenado por mayor número de veces {índice:'palabra'}
other_top_words diccionario ordenado por mayor número de veces {'palabra':índice}
words_list      lista con las n palabras más frecuentes --> list(other_top_words.keys())
dict_ceros      diccionario con las palabras representadas vectorialmente {'palabra':np.array()}


s.voc           diccionario {collection_id: other_top_words}
s.ivoc          diccionario {collection_id: top_words}
s.collections   diccionario {collection_id: lista --> listas (cada una es un texto) --> listas (palabras en cada línea)}
s.vrm           diccionario {collection_id: np.array() (esta es la matriz)}
s.reduced_vrm   diccionario

Equivalencias para traducción
libro == s.collection[collection_id][book_index] --> Esto es una lista con renglones
collection[collection_id] --> lista con los textos
other_top_words == self.voc[collection_id]

Orden de ejecución
Llamar a read_collection



Atributos y variables de interés
home_dir        .
data_path       ./data/text_comp/texts
fig_path        ./figs
collection_path ./data/text_comp/texts/collection_id  --> Aquí están los libros
file_path       ./data/text_comp/texts/collection_id/file_name --> Este ya es un libro



Métodos
init            Empieza el objeto
                    Crea todos los diccionarios e imprime las collections que encontró
proc_line       Es un método estático
                    Recibe lineas y las limpia. Regresa la lista de las palabras
get_voc         Crea voc y ivoc
                    Necesita collections
read_collection Crea collections
                    Necesita NADA
dist_rep        Crea vrm
                    Necesita voc, collections
ppmi_reweight   Modifica vrm
                    Necesita voc, vrm
dim_redux       Crea reduced_vrm
                    Necesita vrm
plot_reps       Crea las gráficas en figs_path
                    Necesita collections, reduced_vrm, ivoc
make_bulk_co    Crea BULK collection
                    Necesita collections
get_word_rel    Regresa la correlación
                    Necesita voc